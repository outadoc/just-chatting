name: Release - Create tag

on:
  workflow_dispatch:
  schedule:
    - cron: "0 1 * * *"

jobs:
  create-release:
    name: Create release and tag
    runs-on: ubuntu-latest
    environment: release

    concurrency:
      group: create-release
      cancel-in-progress: true

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Check if last commit is tagged
        id: check-tagged-commit
        continue-on-error: true
        run: |
          if [[ $(git tag --points-at HEAD) ]]; then
            echo "Last commit is tagged"
            echo "tagged=yes" >> $GITHUB_STATE
          else
            echo "Last commit is not tagged"
            echo "tagged=no" >> $GITHUB_STATE
          fi

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y.%m')" >> $GITHUB_STATE

      - name: Compute unique version code
        id: version
        run: echo "version=$(git rev-list HEAD --count)" >> $GITHUB_STATE

      - name: Create GitHub release
        uses: ncipollo/release-action@v1.18.0
        if: steps.check-tagged-commit.outputs.tagged == 'yes'
        with:
          tag: "v${{ steps.date.outputs.date }}.${{ steps.version.outputs.version }}"
          commit: main
          makeLatest: true
          generateReleaseNotes: true
          token: ${{ secrets.RELEASE_GITHUB_TOKEN }}
