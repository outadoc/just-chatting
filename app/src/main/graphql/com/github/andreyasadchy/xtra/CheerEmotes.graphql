query CheerEmotes($id: ID) {
    user(id: $id) {
        cheer {
            emotes {
                prefix
                tiers {
                    bits
                    color
                    images {
                        theme
                        isAnimated
                        dpiScale
                        url
                    }
                }
            }
        }
    }
}
