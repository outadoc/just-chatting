query Stream($id: ID) {
    user(id: $id) {
        login
        displayName
        profileImageURL(width: 300)
        stream {
            id
            type
            title
            viewersCount
            previewImageURL
            game {
                id
                displayName
            }
        }
    }
}
