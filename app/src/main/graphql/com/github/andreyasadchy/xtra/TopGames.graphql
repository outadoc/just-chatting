query TopGames($first: Int, $after: Cursor) {
    games(first: $first, after: $after) {
        edges {
            cursor
            node {
                displayName
                avatarURL
                viewersCount
                broadcastersCount
            }
        }
    }
}
