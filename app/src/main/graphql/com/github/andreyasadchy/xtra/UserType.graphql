query UserType($id: [ID!]) {
    users(ids: $id) {
        id
        roles {
            isAffiliate
            isGlobalMod
            isPartner
            isSiteAdmin
            isStaff
        }
    }
}